# ความก้าวหน้า — 7 มิถุนายน 2569

<!-- Claude Code edit: สร้าง doc/INDEX.md เป็นดัชนีเอกสารหลักของโปรเจกต์ + จัดระเบียบเอกสาร -->

## สิ่งที่ดำเนินการในวันนี้

> รอบนี้เป็น **งานเอกสาร** เป็นหลัก — ไม่มีการแก้โค้ดที่ commit

### 1. สร้าง `doc/INDEX.md` (ดัชนีเอกสารทั้งหมด)

โปรเจกต์ยังไม่เคยมีไฟล์ INDEX.md มาก่อน จึงสร้างขึ้นใหม่เป็นจุดเริ่มต้นของเอกสารทั้งหมด ครอบคลุม:

- **เอกสารหลัก** — link ไป CLAUDE.md, deploy-guide.md, deploy/DEPLOY.md, docs/server_deployment_guide.md
- **Timeline การพัฒนา** — ตาราง progress logs (เริ่มจาก 2026-05-07)
- **โครงสร้างระบบ 3 apps** — accounts / dashboard / dashboard_system
- **ตารางฟีเจอร์** พร้อมสถานะ (LDAP, Dashboards, Search, Sync Monitor, Auto Sync, PWA, Export)
- **แหล่งข้อมูล** — MySQL (staff) / Oracle (students, thick mode) / Google Sheets

### 2. ทบทวนสถานะโปรเจกต์ (session เริ่มต้นด้วย `/update-docs`)

- พบว่า git log มี commits ใหม่กว่า progress log ล่าสุด (2026-05-07) ที่ยังไม่ได้บันทึก — งาน Search Feature + Timezone Fix (~25 พ.ค.)
- บันทึกไว้ใน Timeline ของ INDEX.md เป็นหมายเหตุ

---

## ไฟล์ที่แก้ไข/สร้างใหม่

| ไฟล์ | การเปลี่ยนแปลง |
|---|---|
| `doc/INDEX.md` | สร้างใหม่ — ดัชนีเอกสารหลักของโปรเจกต์ |

---

## Commits วันนี้

| Commit | รายละเอียด |
|---|---|
| (docs) | บันทึกความก้าวหน้า 2026-06-07 + เพิ่ม INDEX.md |

---

## การตรวจสอบ

- ✅ ตรวจไฟล์ที่ tracked ใน git แล้ว (`doc/deploy-guide.md`, `doc/progress-2026-05-07.md`) ก่อนใส่ link ใน INDEX.md
- ⏳ ยังไม่ได้แตะการเปลี่ยนแปลงโค้ดที่ค้างอยู่ (CLAUDE.md + aims_project/urls.py — health check endpoint)

---

## งานค้าง

- [ ] **Commit แยก:** การเพิ่ม `/health/` endpoint (CLAUDE.md + `aims_project/urls.py` ถูกแก้แล้วแต่ยังไม่ commit) — เป็นงานโค้ดคนละกองกับ docs
- [ ] เขียน progress log ย้อนหลังของงาน Search Feature + Timezone Fix (~25 พ.ค.) ถ้าต้องการความครบถ้วน
- [ ] ตรวจ chart label ภาษาไทยใน Executive Dashboard (legend แสดง □□□) — ค้างจาก 7 พ.ค.
- [ ] พิจารณา Log rotation สำหรับ sync logs

---

## ภาพรวม Phase ปัจจุบัน

| Phase | สถานะ |
|---|---|
| UI Rewrite (Tailwind) ทุกหน้า | ✅ |
| Search Feature | ✅ |
| Sync Monitor + Auto Sync | ✅ |
| เอกสาร (INDEX + progress logs) | 🔄 กำลังจัดระเบียบ |
| Health check endpoint | ⏳ แก้แล้ว รอ commit |
